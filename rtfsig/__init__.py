"""
rtfsig - quickly find interesting parts of RTF files to signature.

By default only the version is exported.
"""

VERSION_MAJOR = 0
VERSION_MINOR = 1
VERSION_PATCH = 0
VERSION_STRING = ".".join(str(i) for i in [VERSION_MAJOR, VERSION_MINOR, VERSION_PATCH])
